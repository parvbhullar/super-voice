---
phase: 05-routing-translation-manipulation
plan: 04
type: execute
wave: 2
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - tests/routing_integration.rs
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-03
  - ROUT-04
  - ROUT-05
  - ROUT-06
  - ROUT-07
  - ROUT-08
  - ROUT-09
  - TRNS-01
  - TRNS-02
  - TRNS-03
  - MANP-01
  - MANP-02
  - MANP-03

must_haves:
  truths:
    - "LPM resolves '+1415xxxxxxx' to trunk via '+1415' rule; '+14155' takes priority"
    - "HTTP query rule fetches external URL and routes to trunk from JSON response"
    - "Translation rewrites '0xxxxxxxxxx' to '+44xxxxxxxxxx' on inbound"
    - "Manipulation adds custom header when P-Asserted-Identity matches regex; anti-action removes when no match"
    - "Jump chain of 10 resolves; depth 11 errors"
  artifacts:
    - path: "tests/routing_integration.rs"
      provides: "Integration tests covering all 5 phase success criteria"
      min_lines: 150
  key_links:
    - from: "tests/routing_integration.rs"
      to: "src/routing/engine.rs"
      via: "Tests exercise RoutingEngine::resolve() end-to-end"
      pattern: "RoutingEngine"
    - from: "tests/routing_integration.rs"
      to: "src/translation/engine.rs"
      via: "Tests exercise TranslationEngine::apply()"
      pattern: "TranslationEngine"
    - from: "tests/routing_integration.rs"
      to: "src/manipulation/engine.rs"
      via: "Tests exercise ManipulationEngine::evaluate()"
      pattern: "ManipulationEngine"
---

<objective>
Write integration tests that verify all 5 phase success criteria end-to-end: LPM priority, HTTP query routing, translation rewriting, manipulation conditional headers, and jump depth limits.

Purpose: These tests prove the routing, translation, and manipulation engines work correctly together and match the phase acceptance criteria exactly.

Output: Integration test file that covers every success criterion.
</objective>

<execution_context>
@/Users/parvbhullar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/parvbhullar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-routing-translation-manipulation/05-01-PLAN.md
@.planning/phases/05-routing-translation-manipulation/05-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration tests for all Phase 5 success criteria</name>
  <files>tests/routing_integration.rs</files>
  <behavior>
    - Test SC1: Create routing table with LPM rules "+1" (priority 100), "+1415" (priority 50), "+14155" (priority 10). Resolve "+14155551234" -> matches "+14155" (longest prefix). Resolve "+14161234567" -> matches "+1" (only prefix that fits).
    - Test SC2: Create routing table with HTTP query rule pointing to a mock URL. The mock returns {"trunk": "trunk-from-api"}. Resolve returns "trunk-from-api". (Use a local HTTP server or mock the reqwest response.)
    - Test SC3: Create translation class with destination_pattern "^0(\d{10})$" -> "+44$1", direction "inbound". Apply to input destination "02071234567" with direction "inbound" -> result is "+442071234567". Apply same with direction "outbound" -> no change.
    - Test SC4: Create manipulation class with condition field="P-Asserted-Identity" pattern="^\+1415". Action: set_header "X-Region" = "SF". Anti-action: remove_header "X-Region". Evaluate with PAI="+14155551234" -> set_headers has X-Region=SF. Evaluate with PAI="+442071234567" -> remove_headers has X-Region.
    - Test SC5: Create 11 routing tables, each with a jump_to the next. Resolve on first table with depth limit -> succeeds up to depth 10, fails at depth 11 with max depth error.
  </behavior>
  <action>
  Create `tests/routing_integration.rs` as a Rust integration test file.

  **Test SC1: LPM Priority**
  - Build RoutingTableConfig with three RoutingRecord entries:
    - MatchType::Lpm, value="+1", targets=[RoutingTarget{trunk:"trunk-us", load_percent: None}], priority=100
    - MatchType::Lpm, value="+1415", targets=[RoutingTarget{trunk:"trunk-sf", load_percent: None}], priority=50
    - MatchType::Lpm, value="+14155", targets=[RoutingTarget{trunk:"trunk-sf5", load_percent: None}], priority=10
  - Use the in-memory routing engine (if RoutingEngine requires ConfigStore, either mock it or test via the lower-level lpm_lookup function directly)
  - Assert "+14155551234" -> trunk-sf5
  - Assert "+14161234567" -> trunk-us ("+1" is only match since "+1416" has no specific rule, but "+1" matches)
  - Assert "+442071234567" -> None (no match)

  **Test SC2: HTTP Query**
  - Start a lightweight local HTTP server (use `axum` or `hyper` to spin up a test server on a random port) that responds to GET requests with `{"trunk": "trunk-from-api"}`
  - Create RoutingRecord with MatchType::HttpQuery, value=test server URL
  - Resolve and assert result trunk is "trunk-from-api"
  - If spinning up a server is too complex, test http_query_lookup directly with a mock, and add a comment noting the mock approach

  **Test SC3: Translation Rewrite**
  - Build TranslationClassConfig with rule: destination_pattern="^0(\\d{10})$", destination_replace="+44$1", direction="inbound"
  - Apply with input destination="02071234567", direction="inbound" -> assert result.destination_number == "+442071234567"
  - Apply with input destination="02071234567", direction="outbound" -> assert result.destination_number == "02071234567" (unchanged)
  - Apply with input destination="+15551234567", direction="inbound" -> assert unchanged (no regex match)

  **Test SC4: Manipulation Conditional**
  - Build ManipulationClassConfig with one rule:
    - conditions: [{field: "P-Asserted-Identity", pattern: "^\\+1415"}]
    - condition_mode: "and"
    - actions: [{action_type: "set_header", name: "X-Region", value: "SF"}]
    - anti_actions: [{action_type: "remove_header", name: "X-Region"}]
  - Evaluate with headers={"P-Asserted-Identity": "+14155551234"} -> assert set_headers contains X-Region=SF
  - Evaluate with headers={"P-Asserted-Identity": "+442071234567"} -> assert remove_headers contains "X-Region"

  **Test SC5: Jump Depth Limit**
  - For a unit-level test: build routing data for 11 tables where table-N jumps to table-(N+1)
  - If using RoutingEngine with ConfigStore, set up all 11 tables in Redis (use test-isolated ConfigStore with UUID prefix)
  - Resolve starting at table-0 with destination that matches the jump rule
  - Assert depth 10 chain succeeds (provide a terminal target at table-10)
  - Assert depth 11 chain returns error containing "max depth" (make table-10 also jump to table-11)

  All tests should use `#[tokio::test]` where async is needed.
  </action>
  <verify>
    <automated>cargo test --test routing_integration -- --nocapture 2>&1 | tail -30</automated>
  </verify>
  <done>
  - SC1: LPM resolves with longest prefix priority verified
  - SC2: HTTP query routing returns trunk from external response verified
  - SC3: Translation rewrites "0xxxxxxxxxx" to "+44xxxxxxxxxx" on inbound verified
  - SC4: Manipulation adds/removes header based on condition match verified
  - SC5: Jump depth 10 succeeds, depth 11 errors verified
  - All integration tests pass
  </done>
</task>

</tasks>

<verification>
```bash
cargo test --test routing_integration -- --nocapture
cargo test 2>&1 | tail -10
```
</verification>

<success_criteria>
- All 5 phase success criteria have corresponding passing tests
- Tests are deterministic and don't depend on external services (HTTP query uses local mock)
- cargo test passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/05-routing-translation-manipulation/05-04-SUMMARY.md`
</output>
