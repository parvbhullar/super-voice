---
phase: 12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy
plan: 05
type: execute
wave: 5
depends_on: [12-04]
files_modified:
  - crates/pjsip/tests/outbound_invite.rs
  - src/proxy/pj_failover.rs
  - src/endpoint/sofia_endpoint.rs
autonomous: true
requirements: [PJMIG-05, PJMIG-06]

must_haves:
  truths:
    - "PjBridge outbound INVITE smoke test passes (CreateInvite -> Terminated because no peer)"
    - "PjFailoverLoop unit tests cover NoRoutes, timeout, cancel token, and nofailover code paths"
    - "sofia_endpoint.rs is deleted — no remaining sofia references in src/ (except comments/docs)"
    - "cargo check --features carrier compiles cleanly after cleanup"
    - "cargo check --features minimal compiles cleanly (rsipstack path unaffected)"
  artifacts:
    - path: "crates/pjsip/tests/outbound_invite.rs"
      provides: "Integration test: CreateInvite -> Terminated event on unreachable target"
    - path: "src/proxy/pj_failover.rs"
      provides: "Unit tests for PjFailoverLoop edge cases"
      contains: "#[cfg(test)]"
  key_links:
    - from: "crates/pjsip/tests/outbound_invite.rs"
      to: "crates/pjsip/src/bridge.rs"
      via: "PjBridge::start + send_command(CreateInvite)"
      pattern: "PjBridge::start"
    - from: "src/proxy/pj_failover.rs"
      to: "src/proxy/failover.rs"
      via: "Reuses is_nofailover in tests"
      pattern: "is_nofailover"
---

<objective>
Write integration tests for the pjsip layer and clean up sofia-sip artifacts.

Purpose: Validate that the pjsip stack works end-to-end for outbound INVITEs and that the failover logic handles all edge cases. Then remove the now-unused sofia_endpoint.rs to complete the migration.
Output: Passing test suite + clean codebase with no dead sofia references.
</objective>

<execution_context>
@/Users/parvbhullar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/parvbhullar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-04-01-pjsip-migration.md (PRIMARY — Phases 5 and 6)
@.planning/phases/12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy/12-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write pjsip integration tests and PjFailoverLoop unit tests</name>
  <files>crates/pjsip/tests/outbound_invite.rs, src/proxy/pj_failover.rs</files>
  <action>
Implement design doc Phase 5, Tasks 5.1 and 5.2.

1. Create `crates/pjsip/tests/outbound_invite.rs`:
   - test_outbound_invite_to_unreachable_target:
     a. Start PjBridge on 127.0.0.1:15061 (UDP, no timers/100rel)
     b. Create per-call event channel (unbounded)
     c. Send PjCommand::CreateInvite targeting sip:test@127.0.0.1:19999 (unreachable)
     d. Wait on event_rx with 10s timeout
     e. Expect to receive PjCallEvent::Terminated (because no one answers)
     f. Clean up: send Shutdown, drop bridge
   - test_bridge_handles_multiple_invites:
     a. Start PjBridge
     b. Send 3 CreateInvite commands to different unreachable targets
     c. Verify each per-call channel receives its own Terminated event
     d. Verify CALL_REGISTRY is empty after all calls terminate

2. Add `#[cfg(test)] mod tests` to `src/proxy/pj_failover.rs`:
   - test_no_routes_returns_no_routes:
     Create PjFailoverLoop with a mock/empty trunk (no gateways), call try_routes()
     Assert result is PjFailoverResult::NoRoutes
   - test_extract_user_from_sip_uri:
     Test extract_user("sip:+14155551234@carrier.com") == "+14155551234"
     Test extract_user("sips:user@host") == "user"
     Test extract_user("bareuser") == "bareuser"
   - test_nofailover_reuse:
     Verify is_nofailover(403, trunk_with_no_nofailover_list) returns true (default behavior)
     Verify is_nofailover(503, trunk) returns false (503 should be retried)

Note: Full end-to-end tests with actual SIP peers require real SIP infrastructure and are not feasible in unit test context (same constraint as Phase 6 of the original build — see STATE.md decision). The outbound_invite test validates the pjsip bridge mechanics.
  </action>
  <verify>
    <automated>cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice && cargo test -p pjsip --test outbound_invite -- --nocapture 2>&1 | tail -15 && cargo test --features carrier -p super-voice -- pj_failover --nocapture 2>&1 | tail -15</automated>
  </verify>
  <done>Outbound INVITE test passes (CreateInvite -> Terminated on unreachable target). PjFailoverLoop unit tests pass (NoRoutes, extract_user, nofailover). Per-call event isolation verified.</done>
</task>

<task type="auto">
  <name>Task 2: Remove sofia_endpoint.rs and clean up sofia references</name>
  <files>src/endpoint/sofia_endpoint.rs</files>
  <action>
Implement design doc Phase 6, Task 6.1.

1. Delete `src/endpoint/sofia_endpoint.rs`:
   ```bash
   git rm src/endpoint/sofia_endpoint.rs
   ```

2. Search for remaining sofia references in src/:
   ```bash
   grep -r "sofia" src/ --include="*.rs" -l
   ```
   - Remove any remaining `use` statements referencing sofia_sip or SofiaEndpoint
   - Keep comments/docs that mention sofia-sip for historical context (e.g., "replaced sofia-sip")
   - Do NOT delete crates/sofia-sip/ or crates/sofia-sip-sys/ — keep in workspace for rollback reference (they won't compile unless someone re-adds them to carrier feature)

3. Verify clean compilation:
   ```bash
   cargo check --features carrier
   cargo check --features minimal
   ```

4. Search for any dead imports in the entire src/ that may have been orphaned:
   ```bash
   grep -r "SofiaEndpoint\|SofiaBridge\|SofiaCommand\|SofiaEvent\|sofia_endpoint" src/ --include="*.rs"
   ```
   Remove any matches found.
  </action>
  <verify>
    <automated>cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice && cargo check --features carrier 2>&1 | tail -5 && cargo check --features minimal 2>&1 | tail -5 && grep -r "SofiaEndpoint\|sofia_endpoint" src/ --include="*.rs" | grep -v "//\|replaced\|former" | head -5; echo "grep exit: $?"</automated>
  </verify>
  <done>sofia_endpoint.rs is deleted. No remaining functional references to SofiaEndpoint/sofia_sip in src/. Both carrier and minimal features compile cleanly. Sofia crates remain in workspace as rollback reference.</done>
</task>

</tasks>

<verification>
- `cargo test -p pjsip` — all pjsip crate tests pass (smoke + outbound_invite)
- `cargo test --features carrier` — PjFailoverLoop tests pass
- `cargo check --features carrier` — clean compilation
- `cargo check --features minimal` — clean compilation
- `src/endpoint/sofia_endpoint.rs` does not exist
- `grep -r "SofiaEndpoint" src/ --include="*.rs"` returns no functional references
</verification>

<success_criteria>
The pjsip migration is complete. All tests pass, both feature flags compile cleanly, and sofia_endpoint.rs is removed. The codebase uses pjsip for carrier SIP signaling and rsipstack for WebRTC/WS bridges.
</success_criteria>

<output>
After completion, create `.planning/phases/12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy/12-05-SUMMARY.md`
</output>
