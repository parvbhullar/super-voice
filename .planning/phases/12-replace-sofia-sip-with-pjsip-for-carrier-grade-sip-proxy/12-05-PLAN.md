---
phase: 12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy
plan: 05
type: execute
wave: 5
depends_on: [12-04]
files_modified:
  - crates/pjsip/tests/outbound_invite.rs
  - src/proxy/pj_failover.rs
  - src/endpoint/sofia_endpoint.rs
autonomous: true
requirements: [PJMIG-06]

must_haves:
  truths:
    - "PjBridge smoke test passes (bridge start + shutdown)"
    - "Outbound INVITE test sends INVITE and receives Terminated event"
    - "PjFailoverLoop unit tests cover NoRoutes, timeout, cancel, nofailover"
    - "sofia_endpoint.rs is removed from src/endpoint/"
    - "No functional sofia references remain in src/ (only comments/docs)"
    - "cargo check --features carrier compiles clean"
    - "cargo check --features minimal compiles clean"
  artifacts:
    - path: "crates/pjsip/tests/outbound_invite.rs"
      provides: "Integration test for outbound INVITE via PjBridge"
    - path: "src/proxy/pj_failover.rs"
      provides: "Unit tests in #[cfg(test)] mod tests"
  key_links:
    - from: "crates/pjsip/tests/outbound_invite.rs"
      to: "crates/pjsip/src/bridge.rs"
      via: "PjBridge::start + send_command(CreateInvite)"
      pattern: "PjBridge::start"
---

<objective>
Integration tests verifying pjsip proxy path works end-to-end, plus cleanup of sofia-sip references.

Purpose: Validate that the pjsip migration is complete and functional. Tests verify PjBridge can send outbound INVITEs, PjFailoverLoop handles all edge cases, and no sofia-sip code remains in the active source. Sofia crates stay in workspace for rollback but are not compiled by default.
Output: Passing test suite, clean carrier build, sofia_endpoint.rs removed.
</objective>

<execution_context>
@/Users/parvbhullar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/parvbhullar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-04-01-pjsip-migration.md
@.planning/phases/12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy/12-CONTEXT.md
@.planning/phases/12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy/12-RESEARCH.md
@.planning/phases/12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy/12-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration tests for pjsip proxy path</name>
  <files>crates/pjsip/tests/outbound_invite.rs, src/proxy/pj_failover.rs</files>
  <action>
1. Create `crates/pjsip/tests/outbound_invite.rs`:
   - Test: start PjBridge on 127.0.0.1:15061 UDP (use unique port to avoid conflicts)
   - Send CreateInvite command to a non-existent target (e.g. sip:test@127.0.0.1:19999)
   - Receive event on the per-call rx channel
   - Assert: receives PjCallEvent::Terminated (because nothing answers at that port)
   - Assert: code is 408 (timeout) or 503 depending on pjsip behavior for unreachable target
   - Send Shutdown and drop bridge
   - Use short timeout (5s) for the test

2. Add unit tests to `src/proxy/pj_failover.rs` in `#[cfg(test)] mod tests`:

   Test: pj_failover_no_routes
   - Create a TrunkConfig with empty gateways vec
   - Call try_routes() (need to mock PjDialogLayer -- since PjDialogLayer requires Arc<PjBridge>, create a test helper that constructs a mock or use a real bridge on a test port)
   - Assert result is PjFailoverResult::NoRoutes

   Test: extract_user_parses_sip_uri
   - "sip:+14155551234@carrier.com" -> "+14155551234"
   - "sips:user@host" -> "user"
   - "plain" -> "plain"

   Test: is_nofailover_reuse (already covered by existing failover.rs tests, but verify import works)
   - Call crate::proxy::failover::is_nofailover(403, &trunk_with_nofailover_codes) -> true
   - Call crate::proxy::failover::is_nofailover(503, &trunk_with_nofailover_codes) -> false

   Note: Full end-to-end proxy tests (SIP-to-SIP bridging through pjsip) require real SIP infrastructure per Phase 6 decision "Full SIP-stack end-to-end tests not feasible without real SIP infrastructure". Component-level tests are sufficient.
  </action>
  <verify>
    <automated>cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice && cargo test -p pjsip --test smoke --test outbound_invite -- --nocapture 2>&1 | tail -10</automated>
  </verify>
  <done>Smoke test and outbound INVITE test pass. PjFailoverLoop unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Remove sofia_endpoint.rs and verify clean build</name>
  <files>src/endpoint/sofia_endpoint.rs</files>
  <action>
1. Delete `src/endpoint/sofia_endpoint.rs` (git rm).

2. Verify no functional sofia references remain in src/:
   - Run: `grep -rn "sofia" src/ --include="*.rs" | grep -v "//\|///\|#\[doc\|README\|comment\|backward"` to find any non-comment references
   - Any remaining "sofia" references should be in comments explaining backward compatibility (e.g., "accepts stack='sofia' for backward compat") -- these are acceptable
   - If any functional code references SofiaEndpoint or sofia_endpoint module, fix it

3. Verify both feature builds compile:
   - `cargo check --features carrier` -- should compile clean (pjsip path)
   - `cargo check --features minimal` -- should compile clean (rsipstack only)
   - `cargo check` -- should compile clean (default features = carrier)

4. Verify sofia-sip crates still exist in workspace but are not compiled:
   - `ls crates/sofia-sip/` and `ls crates/sofia-sip-sys/` -- should still exist
   - `cargo check -p sofia-sip` -- may or may not compile (acceptable either way, they are just kept for reference)

5. Run the full pjsip test suite:
   - `cargo test -p pjsip` -- all tests pass
   - `cargo test --features carrier -p super-voice --lib` -- existing tests still pass (no regression)
  </action>
  <verify>
    <automated>cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice && cargo check --features carrier 2>&1 | tail -3 && cargo check --features minimal 2>&1 | tail -3 && test ! -f src/endpoint/sofia_endpoint.rs && echo "sofia_endpoint.rs removed"</automated>
  </verify>
  <done>sofia_endpoint.rs is deleted. No functional sofia references in src/. Both carrier and minimal features compile clean. All pjsip tests pass.</done>
</task>

</tasks>

<verification>
- `cargo test -p pjsip` -- all tests pass (smoke + outbound_invite)
- `cargo check --features carrier` -- compiles clean
- `cargo check --features minimal` -- compiles clean
- `src/endpoint/sofia_endpoint.rs` does not exist
- `grep -rn "SofiaEndpoint" src/` returns no results
</verification>

<success_criteria>
- PjBridge smoke test passes (start + shutdown)
- Outbound INVITE test sends INVITE and receives Terminated event
- PjFailoverLoop unit tests cover NoRoutes and URI parsing
- sofia_endpoint.rs is removed
- No functional sofia references remain in src/
- Both carrier and minimal features compile clean
</success_criteria>

<output>
After completion, create `.planning/phases/12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy/12-05-SUMMARY.md`
</output>
