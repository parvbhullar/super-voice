---
phase: 12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/install-pjproject.sh
  - crates/pjsip-sys/Cargo.toml
  - crates/pjsip-sys/pjsip_wrapper.h
  - crates/pjsip-sys/build.rs
  - crates/pjsip-sys/src/lib.rs
autonomous: true
requirements: [PJMIG-01]

must_haves:
  truths:
    - "pjsip-sys crate compiles via cargo check -p pjsip-sys"
    - "bindgen generates FFI bindings from pjproject 2.14.x headers"
    - "pkg-config locates libpjproject at build time"
  artifacts:
    - path: "scripts/install-pjproject.sh"
      provides: "pjproject build-from-source script"
    - path: "crates/pjsip-sys/Cargo.toml"
      provides: "Crate manifest with bindgen + pkg-config build-deps"
      contains: "links = \"pjsip\""
    - path: "crates/pjsip-sys/build.rs"
      provides: "bindgen generation with B2BUA-scoped allowlists"
      contains: "pjsip_inv_"
    - path: "crates/pjsip-sys/pjsip_wrapper.h"
      provides: "C header aggregating pjsip includes"
      contains: "sip_inv.h"
    - path: "crates/pjsip-sys/src/lib.rs"
      provides: "Re-export of generated bindings"
      contains: "bindings.rs"
  key_links:
    - from: "crates/pjsip-sys/build.rs"
      to: "libpjproject.pc"
      via: "pkg-config probe"
      pattern: "probe.*libpjproject"
---

<objective>
Create the pjsip-sys crate with raw FFI bindings to pjproject 2.14.x via bindgen.

Purpose: Foundation layer -- all safe pjsip wrappers depend on these raw bindings. Uses pkg-config to locate pjproject and bindgen with B2BUA-specific allowlists (no opaque_type, struct fields accessible).
Output: `crates/pjsip-sys/` crate that compiles and generates bindings, plus install script for pjproject.
</objective>

<execution_context>
@/Users/parvbhullar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/parvbhullar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-04-01-pjsip-migration.md
@.planning/phases/12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy/12-CONTEXT.md
@.planning/phases/12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy/12-RESEARCH.md

<interfaces>
<!-- Existing pattern to replicate (adapted, not copied): -->
<!-- crates/sofia-sip-sys/build.rs uses bindgen + pkg-config -->
<!-- crates/sofia-sip-sys/Cargo.toml uses links = "sofia-sip" -->

<!-- Key differences from sofia-sip-sys: -->
<!-- 1. Do NOT use .opaque_type(".*") -- pjsip struct fields must be accessible -->
<!-- 2. Use derive_debug(true) and derive_default(true) for callback structs -->
<!-- 3. Probe "libpjproject" not "sofia-sip-ua" -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create install script and pjsip-sys crate</name>
  <files>scripts/install-pjproject.sh, crates/pjsip-sys/Cargo.toml, crates/pjsip-sys/pjsip_wrapper.h, crates/pjsip-sys/build.rs, crates/pjsip-sys/src/lib.rs</files>
  <action>
Create `scripts/install-pjproject.sh` that builds pjproject 2.14.1 from source with SIP-only config (disable video, sound, v4l2, amr, silk, bcg729, libyuv, libwebrtc; enable shared; handle macOS OpenSSL with `--with-ssl=$(brew --prefix openssl)` fallback). Install to PREFIX (default /usr/local). Include `make dep && make -j$(nproc || sysctl -n hw.ncpu)`.

Create `crates/pjsip-sys/Cargo.toml` with:
- `links = "pjsip"`
- build-deps: `bindgen = "0.71"`, `pkg-config = "0.3"`
- deps: `libc = "0.2"`

Create `crates/pjsip-sys/pjsip_wrapper.h` including ONLY B2BUA-relevant headers:
- pjlib: types.h, pool.h, log.h, os.h, string.h, timer.h, errno.h
- pjlib-util: resolver.h, srv_resolver.h, dns.h
- pjsip core: sip_transport.h, sip_transaction.h, sip_endpoint.h, sip_module.h, sip_event.h, sip_msg.h, sip_uri.h, sip_auth.h, sip_dialog.h, sip_ua_layer.h, sip_util.h, sip_resolve.h
- pjsip-ua: sip_inv.h, sip_regc.h, sip_replaces.h, sip_xfer.h, sip_100rel.h, sip_timer.h
- pjsip-simple: evsub.h
- pjmedia SDP only: sdp.h, sdp_neg.h

Create `crates/pjsip-sys/build.rs` using pkg-config probe for `libpjproject` (atleast_version "2.14"). Generate bindings with explicit allowlists for:
- pjlib: pj_init, pj_shutdown, pj_pool_*, pj_caching_pool_*, pj_thread_*, pj_log_*, pj_str, pj_strerror + related types (pj_pool_t, pj_pool_factory, pj_caching_pool, pj_str_t, pj_status_t, pj_bool_t, pj_thread_t)
- pjsip endpoint: pjsip_endpt_* + types (pjsip_endpoint, pjsip_host_port)
- pjsip transport: pjsip_udp_transport_start*, pjsip_tcp_transport_start*, pjsip_tls_transport_start* + types (pjsip_transport.*, pjsip_tls_setting, pjsip_tpfactory)
- pjsip transaction: pjsip_tsx_* + type pjsip_transaction
- pjsip module: pjsip_endpt_register_module, pjsip_endpt_unregister_module + type pjsip_module
- pjsip message: types pjsip_msg.*, pjsip_hdr.*, pjsip_generic_string_hdr, pjsip_uri, pjsip_sip_uri, pjsip_method.*, pjsip_status_code, pjsip_rx_data, pjsip_tx_data + functions pjsip_msg_*, pjsip_hdr_*, pjsip_generic_string_hdr_*, pjsip_parse_uri, pjsip_uri_print
- pjsip-ua dialog: pjsip_dlg_* + types pjsip_dialog, pjsip_role_e
- pjsip-ua INVITE: pjsip_inv_* + types pjsip_inv_session, pjsip_inv_state, pjsip_inv_callback
- pjsip-ua timer: pjsip_timer_* + type pjsip_timer_setting
- pjsip-ua 100rel: pjsip_100rel_*
- pjsip-ua replaces: pjsip_replaces_* + type pjsip_replaces_hdr
- pjsip-ua transfer: pjsip_xfer_*
- pjsip-ua regc: pjsip_regc_* + type pjsip_regc
- pjsip auth: pjsip_auth_* + types pjsip_auth_clt_pref, pjsip_cred_info
- pjsip resolver: pjsip_resolve, pjsip_endpt_resolve + types pjsip_resolve_callback, pjsip_server_addresses
- pjlib-util DNS: pj_dns_resolver_* + type pj_dns_resolver
- pjmedia SDP: pjmedia_sdp_* + types pjmedia_sdp_session, pjmedia_sdp_media, pjmedia_sdp_attr, pjmedia_sdp_conn, pjmedia_sdp_neg.*
- pjsip-simple: pjsip_evsub_* + types pjsip_evsub.*

CRITICAL: Do NOT use `.opaque_type(".*")`. Use `.derive_debug(true)` and `.derive_default(true)`.

Create `crates/pjsip-sys/src/lib.rs` with `#![allow(non_upper_case_globals, non_camel_case_types, non_snake_case, dead_code, clippy::all)]` and `include!(concat!(env!("OUT_DIR"), "/bindings.rs"))`.

Reference the full code in `docs/plans/2026-04-01-pjsip-migration.md` Phase 1 Tasks 1.1 and 1.2 for exact implementations.
  </action>
  <verify>
    <automated>cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice && cargo check -p pjsip-sys 2>&1 | tail -5</automated>
  </verify>
  <done>pjsip-sys crate compiles, bindgen generates bindings from pjproject headers, pkg-config locates libpjproject</done>
</task>

</tasks>

<verification>
- `cargo check -p pjsip-sys` compiles without errors
- `pkg-config --modversion libpjproject` returns 2.14.x
- Generated bindings contain `pjsip_inv_session`, `pjsip_endpt_create`, `pjmedia_sdp_parse`
</verification>

<success_criteria>
- pjsip-sys crate exists in crates/pjsip-sys/ with Cargo.toml, build.rs, wrapper header, and lib.rs
- The crate compiles and generates FFI bindings to pjproject 2.14.x
- Struct fields are accessible (not opaque) in generated bindings
</success_criteria>

<output>
After completion, create `.planning/phases/12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy/12-01-SUMMARY.md`
</output>
