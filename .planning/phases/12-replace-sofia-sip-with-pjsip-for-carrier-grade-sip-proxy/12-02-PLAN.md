---
phase: 12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - crates/pjsip/Cargo.toml
  - crates/pjsip/src/lib.rs
  - crates/pjsip/src/error.rs
  - crates/pjsip/src/pool.rs
  - crates/pjsip/src/event.rs
  - crates/pjsip/src/command.rs
  - crates/pjsip/src/endpoint.rs
  - crates/pjsip/src/session.rs
  - crates/pjsip/src/bridge.rs
  - crates/pjsip/tests/smoke.rs
autonomous: true
requirements: [PJMIG-02]

must_haves:
  truths:
    - "pjsip crate compiles via cargo check -p pjsip"
    - "PjBridge starts a dedicated OS thread running pjsip_endpt_handle_events"
    - "PjBridge shuts down cleanly when PjCommand::Shutdown is sent"
    - "Per-call event isolation works via CALL_REGISTRY HashMap"
    - "PjCallEvent::Confirmed carries call_id for BYE routing post-connect"
    - "respond_to_invite parses SDP via pjmedia_sdp_parse before passing to pjsip_inv_answer"
    - "pj_thread_register is called at start of pjsip OS thread"
  artifacts:
    - path: "crates/pjsip/src/error.rs"
      provides: "PjStatus wrapper and check_status()"
      exports: ["PjStatus", "check_status"]
    - path: "crates/pjsip/src/pool.rs"
      provides: "CachingPool and Pool wrappers"
      exports: ["CachingPool", "Pool"]
    - path: "crates/pjsip/src/event.rs"
      provides: "PjCallEvent enum with Confirmed{call_id, sdp} variant"
      exports: ["PjCallEvent", "PjCallEventSender", "PjCallEventReceiver"]
    - path: "crates/pjsip/src/command.rs"
      provides: "PjCommand enum and PjCredential"
      exports: ["PjCommand", "PjCredential"]
    - path: "crates/pjsip/src/endpoint.rs"
      provides: "PjEndpoint with module init and transport binding"
      exports: ["PjEndpoint", "PjEndpointConfig"]
    - path: "crates/pjsip/src/session.rs"
      provides: "CALL_REGISTRY and CallEntry for per-call isolation"
    - path: "crates/pjsip/src/bridge.rs"
      provides: "PjBridge OS thread + command dispatch + callbacks"
      exports: ["PjBridge"]
    - path: "crates/pjsip/tests/smoke.rs"
      provides: "Smoke test verifying bridge start/shutdown"
  key_links:
    - from: "crates/pjsip/src/bridge.rs"
      to: "crates/pjsip/src/session.rs"
      via: "CALL_REGISTRY lookup in callbacks"
      pattern: "CALL_REGISTRY\\.lock"
    - from: "crates/pjsip/src/bridge.rs"
      to: "crates/pjsip/src/endpoint.rs"
      via: "PjEndpoint::create on pjsip thread"
      pattern: "PjEndpoint::create"
    - from: "crates/pjsip/src/bridge.rs"
      to: "crates/pjsip/src/event.rs"
      via: "event_tx.send(PjCallEvent::*) in callbacks"
      pattern: "PjCallEvent::"
---

<objective>
Create the pjsip safe wrapper crate with all modules: error, pool, event, command, endpoint, session, bridge.

Purpose: Safe Rust abstraction over pjsip C API. The PjBridge runs a dedicated OS thread with pjsip_endpt_handle_events, bridges to Tokio via mpsc channels, and provides per-call event isolation via CALL_REGISTRY. This plan addresses all 4 research gaps: (1) call_id in Confirmed event, (2) SDP parsing in respond_to_invite, (3) pj_thread_register at thread start, (4) explicit bridge API for AppState wiring.
Output: `crates/pjsip/` crate with smoke test passing.
</objective>

<execution_context>
@/Users/parvbhullar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/parvbhullar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-04-01-pjsip-migration.md
@.planning/phases/12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy/12-CONTEXT.md
@.planning/phases/12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy/12-RESEARCH.md
@.planning/phases/12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy/12-01-SUMMARY.md

<interfaces>
<!-- From pjsip-sys (Plan 01 output): -->
<!-- All pjsip_sys:: types and functions are available via include!(bindings.rs) -->
<!-- Key functions: pj_init, pj_shutdown, pj_pool_create, pj_pool_release -->
<!-- pj_caching_pool_init, pj_caching_pool_destroy, pj_thread_register -->
<!-- pjsip_endpt_create, pjsip_endpt_handle_events, pjsip_endpt_destroy -->
<!-- pjsip_ua_init_module, pjsip_inv_usage_init, pjsip_100rel_init_module -->
<!-- pjsip_timer_init_module, pjsip_replaces_init_module -->
<!-- pjsip_dlg_create_uac, pjsip_inv_create_uac, pjsip_inv_invite -->
<!-- pjsip_inv_send_msg, pjsip_inv_answer, pjsip_inv_end_session -->
<!-- pjsip_auth_clt_set_credentials, pjsip_timer_init_session -->
<!-- pjmedia_sdp_parse, pjmedia_sdp_neg_get_active_remote, pjmedia_sdp_print -->
<!-- pjsip_generic_string_hdr_create, pjsip_msg_add_hdr -->

<!-- Existing bridge pattern to follow: -->
<!-- crates/sofia-sip/src/bridge.rs: OS thread + mpsc + tight event loop -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pjsip crate core types (error, pool, event, command)</name>
  <files>crates/pjsip/Cargo.toml, crates/pjsip/src/lib.rs, crates/pjsip/src/error.rs, crates/pjsip/src/pool.rs, crates/pjsip/src/event.rs, crates/pjsip/src/command.rs</files>
  <action>
Create `crates/pjsip/Cargo.toml` with deps: pjsip-sys (path), libc, anyhow, tokio (sync+rt+macros), tracing, once_cell, uuid (v4 feature).

Create `crates/pjsip/src/error.rs`: PjStatus(i32) wrapper with is_ok(), message() via pj_strerror, Display impl, std::error::Error impl. `check_status(i32) -> Result<(), PjStatus>`.

Create `crates/pjsip/src/pool.rs`: CachingPool wrapping pj_caching_pool (new(), factory_ptr(), create_pool(name, initial, increment) -> Pool, Drop impl calls pj_caching_pool_destroy). Pool wrapping pj_pool_t (as_ptr(), Drop calls pj_pool_release, unsafe impl Send).

Create `crates/pjsip/src/event.rs`: PjCallEvent enum with variants:
- Trying
- Ringing { status: u16 }
- EarlyMedia { status: u16, sdp: String }
- Confirmed { call_id: String, sdp: Option<String> }  // RESEARCH GAP FIX: call_id field added so BYE can be routed post-connect
- Terminated { code: u16, reason: String }
- Info { content_type: String, body: String }
- ReInvite { sdp: String }
Plus type aliases: PjCallEventSender = tokio::sync::mpsc::UnboundedSender<PjCallEvent>, PjCallEventReceiver = tokio::sync::mpsc::UnboundedReceiver<PjCallEvent>.

Create `crates/pjsip/src/command.rs`: PjCommand enum with variants AcceptIncoming{call_id, event_tx}, Respond{call_id, status, reason, sdp: Option<String>}, CreateInvite{uri, from, sdp, event_tx, credential: Option<PjCredential>, headers: Option<Vec<(String,String)>>}, Bye{call_id}, Shutdown. PjCredential struct with realm, username, password, scheme fields.

Create `crates/pjsip/src/lib.rs` re-exporting all public types.

Reference `docs/plans/2026-04-01-pjsip-migration.md` Phase 2 Tasks 2.1 and 2.2 for exact implementations.
  </action>
  <verify>
    <automated>cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice && cargo check -p pjsip 2>&1 | tail -5</automated>
  </verify>
  <done>Core types compile. PjCallEvent::Confirmed includes call_id field (research gap 1 fixed).</done>
</task>

<task type="auto">
  <name>Task 2: Create PjEndpoint, PjBridge, session registry, and smoke test</name>
  <files>crates/pjsip/src/endpoint.rs, crates/pjsip/src/session.rs, crates/pjsip/src/bridge.rs, crates/pjsip/tests/smoke.rs</files>
  <action>
Create `crates/pjsip/src/endpoint.rs`: PjEndpointConfig struct (bind_addr, port, transport, tls_cert_file, tls_privkey_file, session_timers, session_expires, min_se, enable_100rel) with Default impl (0.0.0.0:5060, udp, timers=true, 1800s/90s, 100rel=true). PjEndpoint struct (endpt ptr, caching_pool, pool ptr, config). PjEndpoint::create(config) initializes pj_init, CachingPool, pjsip_endpt_create, creates main pool, calls init_modules() and start_transport(). init_modules() registers: pjsip_ua_init_module, pjsip_inv_usage_init (with on_state_changed and on_new_session callbacks from bridge module), pjsip_100rel_init_module, pjsip_timer_init_module, pjsip_replaces_init_module. start_transport() dispatches to start_udp/tcp/tls. handle_events(max_timeout_ms) calls pjsip_endpt_handle_events, treats PJ_ETIMEDOUT (120004) as non-error. Drop calls pjsip_endpt_destroy then pj_shutdown. unsafe impl Send.

Create `crates/pjsip/src/session.rs`: CALL_REGISTRY as Lazy<Mutex<HashMap<String, CallEntry>>>. CallEntry { event_tx: PjCallEventSender, inv_ptr: *mut pjsip_inv_session }. unsafe impl Send for CallEntry. PjInvSession { call_id: String } for external use.

Create `crates/pjsip/src/bridge.rs`: PjBridge { cmd_tx, thread_handle }. PjBridge::start(config) spawns named "pjsip" OS thread running pjsip_thread_main. send_command() sends PjCommand via channel. Drop sends Shutdown + joins thread.

pjsip_thread_main(config, cmd_rx):
- RESEARCH GAP FIX 3: Call pj_thread_register("pjsip", desc, &mut thread) FIRST before PjEndpoint::create. Allocate a static-lifetime thread descriptor (Box::leak a zeroed array of appropriate size for pj_thread_desc).
- Create PjEndpoint
- Tight loop: endpoint.handle_events(5) + drain cmd_rx via try_recv
- On Shutdown or Disconnected: break and drop endpoint

handle_command() dispatches:
- Shutdown: set flag
- CreateInvite: call create_outbound_invite()
- Respond: call respond_to_invite()
- Bye: call send_bye()
- AcceptIncoming: update event_tx in CALL_REGISTRY

create_outbound_invite(endpoint, uri, from, sdp_str, event_tx, credential, headers):
- Create CStrings for all string params (keep alive in scope -- PITFALL 1 from research)
- pjsip_dlg_create_uac (ua_instance, from, contact, to, target)
- If credential: set via pjsip_auth_clt_set_credentials on dialog
- Parse SDP via pjmedia_sdp_parse(endpoint.pool, sdp_cstr, len, &mut sdp_session)
- pjsip_inv_create_uac(dlg, sdp_session, 0, &mut inv)
- pjsip_timer_init_session(inv, &timer_setting) with 1800s/90s defaults
- Extract call_id from dialog->call_id->id (fallback to uuid::Uuid::new_v4)
- Register in CALL_REGISTRY (drop lock before any pjsip call -- PITFALL 4)
- pjsip_inv_invite(inv, &mut tdata) + add custom headers + pjsip_inv_send_msg

respond_to_invite(call_id, status_code, reason, sdp, endpoint):
- RESEARCH GAP FIX 2: When sdp is Some, parse it via pjmedia_sdp_parse(endpoint.pool, sdp_cstr, len, &mut sdp_session) and pass the parsed pjmedia_sdp_session* as the local_sdp parameter to pjsip_inv_answer. When sdp is None, pass ptr::null_mut() for local_sdp.
- NOTE: respond_to_invite needs access to endpoint.pool for SDP parsing. Pass endpoint reference.
- Lookup inv_ptr from CALL_REGISTRY (drop lock before pjsip calls)
- pjsip_inv_answer(inv, status_code, ptr::null(), local_sdp, &mut tdata)
- pjsip_inv_send_msg(inv, tdata)

send_bye(call_id):
- Lookup inv_ptr from CALL_REGISTRY (drop lock before pjsip calls)
- pjsip_inv_end_session(inv, 200, ptr::null(), &mut tdata)
- pjsip_inv_send_msg(inv, tdata) if tdata not null
- Remove from CALL_REGISTRY

on_inv_state_changed callback (extern "C"):
- Extract call_id via extract_call_id(inv)
- Look up event_tx from CALL_REGISTRY (clone, drop lock before send)
- Map states: CALLING->Trying, EARLY->(check SDP: EarlyMedia or Ringing), CONFIRMED->Confirmed{call_id: call_id.clone(), sdp} (RESEARCH GAP FIX 1), DISCONNECTED->Terminated (+ remove from registry)
- For EARLY state SDP: read from pjsip_event body.tsx_state.src.rdata message body (PITFALL 5 -- do NOT use pjmedia_sdp_neg_get_active_remote for early states)
- For CONFIRMED state SDP: use pjmedia_sdp_neg_get_active_remote + pjmedia_sdp_print

on_inv_new_session callback: register placeholder in CALL_REGISTRY for incoming calls.

extract_call_id(inv): inv->dlg->call_id->id as String.
extract_response_info(inv): get status code and SDP from negotiator or response body.

Create smoke test `crates/pjsip/tests/smoke.rs`: start PjBridge on 127.0.0.1:15060 UDP with timers/100rel disabled, send Shutdown, drop bridge, assert no panic.

Reference `docs/plans/2026-04-01-pjsip-migration.md` Phase 2 Tasks 2.3 and 2.4 for exact implementations. Apply all 4 research gap fixes listed above.
  </action>
  <verify>
    <automated>cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice && cargo test -p pjsip --test smoke -- --nocapture 2>&1 | tail -10</automated>
  </verify>
  <done>PjBridge starts and shuts down cleanly. pj_thread_register called at thread start (gap 3). respond_to_invite parses SDP before pjsip_inv_answer (gap 2). PjCallEvent::Confirmed carries call_id (gap 1). Smoke test passes.</done>
</task>

</tasks>

<verification>
- `cargo check -p pjsip` compiles without errors
- `cargo test -p pjsip --test smoke` passes
- PjCallEvent::Confirmed has `call_id: String` field (research gap 1)
- respond_to_invite uses pjmedia_sdp_parse for SDP before pjsip_inv_answer (research gap 2)
- pjsip_thread_main calls pj_thread_register before PjEndpoint::create (research gap 3)
</verification>

<success_criteria>
- All 7 modules exist in crates/pjsip/src/ (lib, error, pool, event, command, endpoint, session, bridge)
- PjBridge can start, process commands, deliver per-call events, and shut down cleanly
- All 3 implementation-level research gaps are addressed in the code
- Smoke test verifies bridge lifecycle
</success_criteria>

<output>
After completion, create `.planning/phases/12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy/12-02-SUMMARY.md`
</output>
