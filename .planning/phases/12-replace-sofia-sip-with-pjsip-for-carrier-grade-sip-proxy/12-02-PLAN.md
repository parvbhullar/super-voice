---
phase: 12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - crates/pjsip/Cargo.toml
  - crates/pjsip/src/lib.rs
  - crates/pjsip/src/error.rs
  - crates/pjsip/src/pool.rs
  - crates/pjsip/src/event.rs
  - crates/pjsip/src/command.rs
  - crates/pjsip/src/endpoint.rs
  - crates/pjsip/src/session.rs
  - crates/pjsip/src/bridge.rs
  - crates/pjsip/tests/smoke.rs
autonomous: true
requirements: [PJMIG-02]

must_haves:
  truths:
    - "PjEndpoint initializes pjlib, creates endpoint, binds UDP/TCP/TLS transport, and registers INVITE/100rel/timer/replaces modules"
    - "PjBridge starts a dedicated OS thread running pjsip_endpt_handle_events() with mpsc channel command dispatch"
    - "Per-call event isolation via CALL_REGISTRY — each call gets its own PjCallEventSender/Receiver pair"
    - "PjBridge starts and shuts down cleanly without leaking OS threads (smoke test passes)"
  artifacts:
    - path: "crates/pjsip/src/error.rs"
      provides: "PjStatus wrapper, check_status() helper"
      exports: ["PjStatus", "check_status"]
    - path: "crates/pjsip/src/pool.rs"
      provides: "CachingPool and Pool RAII wrappers"
      exports: ["CachingPool", "Pool"]
    - path: "crates/pjsip/src/event.rs"
      provides: "PjCallEvent enum (Trying, Ringing, EarlyMedia, Confirmed, Terminated, Info, ReInvite)"
      exports: ["PjCallEvent", "PjCallEventSender", "PjCallEventReceiver"]
    - path: "crates/pjsip/src/command.rs"
      provides: "PjCommand enum (AcceptIncoming, Respond, CreateInvite, Bye, Shutdown) + PjCredential"
      exports: ["PjCommand", "PjCredential"]
    - path: "crates/pjsip/src/endpoint.rs"
      provides: "PjEndpoint with transport binding and module init"
      exports: ["PjEndpoint", "PjEndpointConfig"]
    - path: "crates/pjsip/src/session.rs"
      provides: "CALL_REGISTRY, CallEntry, PjInvSession"
      exports: ["PjInvSession"]
    - path: "crates/pjsip/src/bridge.rs"
      provides: "PjBridge OS thread + C callbacks (on_inv_state_changed, on_inv_new_session)"
      exports: ["PjBridge"]
    - path: "crates/pjsip/tests/smoke.rs"
      provides: "Smoke test verifying bridge start/shutdown lifecycle"
  key_links:
    - from: "crates/pjsip/src/bridge.rs"
      to: "crates/pjsip/src/endpoint.rs"
      via: "PjEndpoint::create() called on pjsip thread"
      pattern: "PjEndpoint::create"
    - from: "crates/pjsip/src/bridge.rs"
      to: "crates/pjsip/src/session.rs"
      via: "CALL_REGISTRY for per-call event routing"
      pattern: "CALL_REGISTRY"
    - from: "crates/pjsip/src/bridge.rs"
      to: "crates/pjsip/src/event.rs"
      via: "C callbacks send PjCallEvent through per-call channels"
      pattern: "PjCallEvent"
    - from: "crates/pjsip/src/endpoint.rs"
      to: "crates/pjsip/src/bridge.rs"
      via: "inv_callback references on_inv_state_changed/on_inv_new_session"
      pattern: "on_inv_state_changed"
---

<objective>
Create the pjsip safe Rust wrapper crate with all core modules: error handling, pool management, event/command types, endpoint lifecycle, per-call session tracking, and the OS thread bridge.

Purpose: This crate encapsulates all unsafe pjsip FFI behind a safe Rust API. It's the core building block consumed by PjDialogLayer (Plan 03) and PjsipEndpoint (Plan 04).
Output: `crates/pjsip/` crate with 7 modules, passing smoke test.
</objective>

<execution_context>
@/Users/parvbhullar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/parvbhullar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/plans/2026-04-01-pjsip-migration.md (PRIMARY — contains complete implementation code for all files)
@.planning/phases/12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pjsip crate skeleton with error, pool, event, and command modules</name>
  <files>crates/pjsip/Cargo.toml, crates/pjsip/src/lib.rs, crates/pjsip/src/error.rs, crates/pjsip/src/pool.rs, crates/pjsip/src/event.rs, crates/pjsip/src/command.rs</files>
  <action>
Create the pjsip crate with its first 4 modules as specified in the design doc Phase 2, Tasks 2.1 and 2.2.

1. Create `crates/pjsip/Cargo.toml`:
   - name = "pjsip", edition = "2024"
   - deps: pjsip-sys (path "../pjsip-sys"), libc = "0.2", anyhow = "1", tokio (sync, rt, macros), tracing = "0.1", once_cell = "1", uuid (v4)

2. Create `crates/pjsip/src/error.rs`:
   - PjStatus(i32) wrapper with is_ok(), message() (via pj_strerror), Display, Error impls
   - check_status(i32) -> Result<(), PjStatus>

3. Create `crates/pjsip/src/pool.rs`:
   - CachingPool: wraps pj_caching_pool, new()/factory_ptr()/create_pool(), Drop calls pj_caching_pool_destroy
   - Pool: wraps *mut pj_pool_t, as_ptr(), Drop calls pj_pool_release
   - unsafe impl Send for Pool (used only on pjsip thread)

4. Create `crates/pjsip/src/event.rs`:
   - PjCallEvent enum: Trying, Ringing{status}, EarlyMedia{status, sdp}, Confirmed{sdp: Option}, Terminated{code, reason}, Info{content_type, body}, ReInvite{sdp}
   - type PjCallEventSender = tokio::sync::mpsc::UnboundedSender<PjCallEvent>
   - type PjCallEventReceiver = tokio::sync::mpsc::UnboundedReceiver<PjCallEvent>

5. Create `crates/pjsip/src/command.rs`:
   - PjCommand enum: AcceptIncoming{call_id, event_tx}, Respond{call_id, status, reason, sdp}, CreateInvite{uri, from, sdp, event_tx, credential, headers}, Bye{call_id}, Shutdown
   - PjCredential struct: realm, username, password, scheme

6. Create `crates/pjsip/src/lib.rs`:
   - Declare all 7 modules: bridge, command, endpoint, error, event, pool, session
   - pub use for PjBridge, PjCommand, PjEndpoint, PjStatus, check_status, PjCallEvent, CachingPool, Pool, PjInvSession
   - Module doc explaining the OS thread + mpsc channel architecture
  </action>
  <verify>
    <automated>cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice && cargo check -p pjsip 2>&1 | tail -5</automated>
  </verify>
  <done>crates/pjsip/ has Cargo.toml and 4 core modules (error, pool, event, command) plus lib.rs. cargo check -p pjsip compiles (remaining modules declared but can be empty stubs initially).</done>
</task>

<task type="auto">
  <name>Task 2: Implement PjEndpoint, PjInvSession, PjBridge, and smoke test</name>
  <files>crates/pjsip/src/endpoint.rs, crates/pjsip/src/session.rs, crates/pjsip/src/bridge.rs, crates/pjsip/tests/smoke.rs</files>
  <action>
Implement the remaining 3 modules and a smoke test, as specified in design doc Phase 2, Tasks 2.3 and 2.4.

1. Create `crates/pjsip/src/endpoint.rs`:
   - PjEndpointConfig: bind_addr, port, transport, tls_cert_file, tls_privkey_file, session_timers (default true), session_expires (default 1800), min_se (default 90), enable_100rel (default true)
   - PjEndpoint: holds *mut pjsip_endpoint, CachingPool, *mut pj_pool_t, config
   - PjEndpoint::create(config) -> Result<Self>:
     a. pj_init()
     b. CachingPool::new()
     c. pjsip_endpt_create()
     d. pjsip_endpt_create_pool()
     e. init_modules(): pjsip_ua_init_module, pjsip_inv_usage_init (with on_state_changed + on_new_session callbacks), pjsip_100rel_init_module, pjsip_timer_init_module, pjsip_replaces_init_module
     f. start_transport(): UDP/TCP/TLS based on config
   - handle_events(max_timeout_ms) — calls pjsip_endpt_handle_events, ignores PJ_ETIMEDOUT
   - Drop: pjsip_endpt_destroy + pj_shutdown
   - unsafe impl Send (only used on pjsip thread)

2. Create `crates/pjsip/src/session.rs`:
   - CALL_REGISTRY: Lazy<Mutex<HashMap<String, CallEntry>>>
   - CallEntry: event_tx (PjCallEventSender), inv_ptr (*mut pjsip_inv_session)
   - PjInvSession: call_id (String) — public wrapper

3. Create `crates/pjsip/src/bridge.rs`:
   - PjBridge: cmd_tx (UnboundedSender<PjCommand>), thread_handle
   - PjBridge::start(config) -> Result<Self>: spawn OS thread running pjsip_thread_main
   - PjBridge::send_command() — sends via cmd_tx
   - Drop: sends Shutdown, joins thread
   - pjsip_thread_main(): create PjEndpoint, loop { handle_events(5ms), drain cmd_rx.try_recv() }, break on shutdown
   - handle_command(): dispatch CreateInvite/Respond/Bye/AcceptIncoming/Shutdown
   - create_outbound_invite(): pjsip_dlg_create_uac -> set credentials -> pjmedia_sdp_parse -> pjsip_inv_create_uac -> pjsip_timer_init_session -> register in CALL_REGISTRY -> pjsip_inv_invite -> add custom headers -> pjsip_inv_send_msg
   - respond_to_invite(): lookup inv from CALL_REGISTRY -> pjsip_inv_answer -> pjsip_inv_send_msg
   - send_bye(): lookup inv -> pjsip_inv_end_session -> pjsip_inv_send_msg -> remove from registry
   - extern "C" fn on_inv_state_changed(): map pjsip_inv_state to PjCallEvent, send via per-call event_tx from CALL_REGISTRY
     - CALLING -> Trying
     - EARLY -> Ringing or EarlyMedia (check SDP via pjmedia_sdp_neg_get_active_remote)
     - CONFIRMED -> Confirmed
     - DISCONNECTED -> Terminated (clean up registry)
   - extern "C" fn on_inv_new_session(): create placeholder CallEntry for incoming calls
   - extract_call_id(): reads dialog call_id from inv->dlg->call_id
   - extract_response_info(): reads status code and SDP via pjmedia_sdp_neg_get_active_remote + pjmedia_sdp_print

4. Create `crates/pjsip/tests/smoke.rs`:
   - test_bridge_start_and_shutdown: create PjEndpointConfig (127.0.0.1:15060, udp, no timers/100rel), start PjBridge, send Shutdown, drop
  </action>
  <verify>
    <automated>cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice && cargo test -p pjsip --test smoke -- --nocapture 2>&1 | tail -10</automated>
  </verify>
  <done>All 7 modules of the pjsip crate compile. Smoke test passes: PjBridge starts on a dedicated OS thread, responds to Shutdown command, and joins cleanly without leaking threads.</done>
</task>

</tasks>

<verification>
- `cargo check -p pjsip` compiles all 7 modules
- `cargo test -p pjsip --test smoke` passes
- PjCallEvent variants match what FailoverLoop needs (Trying, Ringing, EarlyMedia, Confirmed, Terminated)
- PjCommand::CreateInvite includes credential, headers, sdp, event_tx
- PjBridge starts/stops cleanly (no thread leak, no panic)
</verification>

<success_criteria>
The pjsip crate provides a complete safe wrapper over pjproject's B2BUA APIs. The OS thread bridge pattern (identical to the existing sofia-sip bridge) is working and tested. Plan 03 can build PjDialogLayer on top of this.
</success_criteria>

<output>
After completion, create `.planning/phases/12-replace-sofia-sip-with-pjsip-for-carrier-grade-sip-proxy/12-02-SUMMARY.md`
</output>
