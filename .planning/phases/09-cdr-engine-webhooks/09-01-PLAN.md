---
phase: 09-cdr-engine-webhooks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cdr/mod.rs
  - src/cdr/types.rs
  - src/cdr/queue.rs
  - src/proxy/dispatch.rs
  - src/redis_state/types.rs
  - src/app.rs
  - src/handler/handler.rs
autonomous: true
requirements:
  - CDRE-01
  - CDRE-02
  - CDRE-03

must_haves:
  truths:
    - "A completed proxy call produces a CarrierCdr with session_id, call_id, uuid, dual-leg correlation"
    - "CDR includes timing fields: start_time, ring_time, answer_time, end_time, billsec"
    - "CDR is pushed to Redis LIST cdr:queue:new and stored as STRING cdr:detail:{uuid} with 3600s TTL"
  artifacts:
    - path: "src/cdr/types.rs"
      provides: "CarrierCdr struct with all carrier fields"
      contains: "pub struct CarrierCdr"
    - path: "src/cdr/queue.rs"
      provides: "CdrQueue for Redis enqueue/dequeue"
      contains: "pub struct CdrQueue"
    - path: "src/cdr/mod.rs"
      provides: "Module re-exports"
    - path: "src/proxy/dispatch.rs"
      provides: "CDR generation after proxy call completion"
      contains: "CarrierCdr"
  key_links:
    - from: "src/proxy/dispatch.rs"
      to: "src/cdr/queue.rs"
      via: "CdrQueue::enqueue after session.run completes"
      pattern: "cdr_queue.*enqueue"
    - from: "src/cdr/queue.rs"
      to: "Redis"
      via: "LPUSH cdr:queue:new + SET cdr:detail:{uuid}"
      pattern: "cdr:queue:new"
---

<objective>
Define the CarrierCdr type with dual-leg correlation and carrier-grade fields, implement a Redis-backed CDR queue, and wire CDR generation into the proxy call dispatch path.

Purpose: Every completed proxy call must produce a structured CDR record that is immediately available in Redis for downstream processing (webhook delivery, API queries).
Output: `src/cdr/` module with types and queue, wired into `dispatch_proxy_call`.
</objective>

<execution_context>
@/Users/parvbhullar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/parvbhullar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/proxy/dispatch.rs
@src/proxy/types.rs
@src/callrecord/mod.rs
@src/app.rs
@src/redis_state/runtime_state.rs

<interfaces>
<!-- Key types the executor needs -->

From src/proxy/types.rs:
```rust
pub struct ProxyCallContext {
    pub session_id: String,
    pub start_time: DateTime<Utc>,
    pub original_caller: String,
    pub original_callee: String,
    pub trunk_name: String,
    pub did_number: Option<String>,
    pub routing_table: Option<String>,
    pub max_forwards: u32,
}

pub enum ProxyCallEvent {
    PhaseChanged(ProxyCallPhase),
    EarlyMedia { sdp: String },
    Answered { sdp: String },
    Terminated { reason: String, code: u16 },
    TransferInitiated { target: String },
    HoldDetected,
    ResumeDetected,
}
```

From src/redis_state/runtime_state.rs:
```rust
pub struct RuntimeState {
    pool: RedisPool,
}
```

From src/app.rs:
```rust
pub struct AppStateInner {
    pub config_store: Option<Arc<ConfigStore>>,
    pub runtime_state: Option<Arc<RuntimeState>>,
    pub capacity_guard: Option<Arc<crate::capacity::guard::CapacityGuard>>,
    // ... other fields
}
pub type AppState = Arc<AppStateInner>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CarrierCdr types and CdrQueue with Redis persistence</name>
  <files>src/cdr/mod.rs, src/cdr/types.rs, src/cdr/queue.rs</files>
  <behavior>
    - Test 1: CarrierCdr serializes to JSON with all required fields (session_id, call_id, uuid, timing, parties, routing, media, status, network, node_id)
    - Test 2: CarrierCdr::billsec() computes seconds between answer_time and end_time; returns 0 when answer_time is None
    - Test 3: CdrQueue::enqueue pushes CDR uuid to Redis LIST "cdr:queue:new" and stores full CDR JSON as STRING "cdr:detail:{uuid}" with 3600s TTL
    - Test 4: CdrQueue::dequeue pops from "cdr:queue:new" LIST and loads full CDR from "cdr:detail:{uuid}"
    - Test 5: CarrierCdr legs (CdrLeg) contain trunk, gateway, codec, transport, srtp, sip_status, hangup_cause fields
  </behavior>
  <action>
Create `src/cdr/` module:

**src/cdr/types.rs:**
- `CarrierCdr` struct with fields per decision: `uuid` (Uuid), `session_id` (String), `call_id` (String), `node_id` (String), `created_at` (DateTime&lt;Utc&gt;), `inbound_leg` (CdrLeg), `outbound_leg` (Option&lt;CdrLeg&gt;), `timing` (CdrTiming), `status` (CdrStatus)
- `CdrTiming` struct: `start_time`, `ring_time`, `answer_time`, `end_time` (all DateTime&lt;Utc&gt; or Option), `billsec` (u64) — computed field
- `CdrLeg` struct: `trunk` (String), `gateway` (Option&lt;String&gt;), `caller` (String), `callee` (String), `codec` (Option&lt;String&gt;), `transport` (String — "udp"/"tcp"/"tls"), `srtp` (bool), `sip_status` (u16), `hangup_cause` (Option&lt;String&gt;), `source_ip` (Option&lt;String&gt;), `destination_ip` (Option&lt;String&gt;)
- `CdrStatus` enum: `Completed`, `Failed`, `Cancelled`, `NoAnswer`, `Busy` — derive Serialize/Deserialize with snake_case
- Implement `CarrierCdr::billsec()` method that computes seconds between answer and end; 0 if no answer
- Implement `CarrierCdr::from_proxy_context(ctx: &ProxyCallContext, ...)` builder that populates fields from session context
- All types derive Debug, Clone, Serialize, Deserialize with serde(rename_all = "snake_case")

**src/cdr/queue.rs:**
- `CdrQueue` struct holding `RedisPool`
- `CdrQueue::new(pool: RedisPool)` constructor
- `async fn enqueue(&self, cdr: &CarrierCdr) -> Result<()>`: serialize CDR to JSON, LPUSH uuid to `cdr:queue:new`, SET full JSON to `cdr:detail:{uuid}` with EX 3600
- `async fn dequeue(&self) -> Result<Option<CarrierCdr>>`: RPOP from `cdr:queue:new`, if Some(uuid) then GET `cdr:detail:{uuid}`, deserialize
- `async fn get(&self, uuid: &str) -> Result<Option<CarrierCdr>>`: GET `cdr:detail:{uuid}`, deserialize
- `async fn queue_len(&self) -> Result<u64>`: LLEN `cdr:queue:new`

**src/cdr/mod.rs:**
- `pub mod types;` `pub mod queue;`
- Re-export `CarrierCdr`, `CdrQueue`, `CdrTiming`, `CdrLeg`, `CdrStatus`
  </action>
  <verify>
    <automated>cargo test --lib cdr:: -- --nocapture</automated>
  </verify>
  <done>CarrierCdr struct serializes/deserializes with all carrier fields; CdrQueue enqueue/dequeue round-trips through Redis; billsec computation correct for answered and unanswered calls</done>
</task>

<task type="auto">
  <name>Task 2: Wire CDR generation into proxy dispatch and AppState</name>
  <files>src/proxy/dispatch.rs, src/app.rs, src/handler/handler.rs</files>
  <action>
**src/app.rs:**
- Add `pub cdr_queue: Option<Arc<crate::cdr::CdrQueue>>` field to `AppStateInner`
- In `AppStateBuilder::build()`, construct `CdrQueue` from the Redis pool when Redis is configured (reuse the same pool pattern as `runtime_state`)
- Add `use crate::cdr;` to module imports

**src/proxy/dispatch.rs:**
- After `session.run()` completes (line ~320-325, after the capacity release), build a `CarrierCdr` from the session context:
  - `uuid`: new Uuid::new_v4()
  - `session_id`: from context
  - `call_id`: from context.session_id (same for now)
  - `node_id`: from `app_state.config.node_id` or hostname
  - `timing.start_time`: from `context.start_time`
  - `timing.end_time`: Utc::now()
  - `timing.ring_time`, `timing.answer_time`: capture from session events (add ring/answer timestamps to the dispatch flow — store Utc::now() when session enters Ringing/Answered phases by consuming `_event_rx`)
  - `timing.billsec`: computed
  - `inbound_leg`: caller info, trunk from DID
  - `outbound_leg`: callee info, trunk from resolved trunk
  - `status`: derive from session result (Ok = Completed, Err with reason = Failed/Cancelled etc.)
- Consume `_event_rx` in a separate tokio::select! branch alongside `session.run()` to capture PhaseChanged/Answered/Terminated events for timing
- If `app_state.cdr_queue` is Some, call `cdr_queue.enqueue(&cdr).await` — log warning on failure but do not fail the dispatch
- Import `crate::cdr::{CarrierCdr, CdrLeg, CdrTiming, CdrStatus}`

**src/handler/handler.rs:**
- No route changes needed yet (CDR query API comes in Plan 03)

**src/main.rs or lib.rs:**
- Add `pub mod cdr;` to the crate module declarations
  </action>
  <verify>
    <automated>cargo test --lib proxy::dispatch -- --nocapture && cargo build</automated>
  </verify>
  <done>Proxy call dispatch generates a CarrierCdr with correct timing fields and enqueues to Redis; build succeeds; existing dispatch unit tests still pass</done>
</task>

</tasks>

<verification>
- `cargo test --lib cdr::` — all CDR type and queue tests pass
- `cargo test --lib proxy::dispatch` — existing dispatch tests still pass
- `cargo build` — no compilation errors
</verification>

<success_criteria>
- CarrierCdr struct exists with all specified fields (uuid, session_id, dual legs, timing, status, network, node_id)
- CdrQueue enqueues to Redis LIST cdr:queue:new and stores detail with TTL
- dispatch_proxy_call generates a CDR after session completion and enqueues it
- billsec correctly computed as seconds between answer and end time
</success_criteria>

<output>
After completion, create `.planning/phases/09-cdr-engine-webhooks/09-01-SUMMARY.md`
</output>
