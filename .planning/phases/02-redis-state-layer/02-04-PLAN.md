---
phase: 02-redis-state-layer
plan: 04
type: execute
wave: 3
gap_closure: true
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/main.rs
autonomous: true
requirements: [RAPI-15]

must_haves:
  truths:
    - "API requests with valid Bearer token succeed; API requests without/invalid Bearer token return 401 Unauthorized (auth middleware runs on LIVE requests, not just in isolated router tests)"
  artifacts:
    - path: "src/main.rs"
      provides: "Main axum router assembly that merges carrier_admin_router so auth middleware runs on live requests"
      contains: ".merge(active_call::handler::carrier_admin_router(app_state.clone()))"
  key_links:
    - from: "src/main.rs"
      to: "src/handler/handler.rs::carrier_admin_router"
      via: "axum Router::merge in main router assembly (lines ~314-321)"
      pattern: "\\.merge\\(active_call::handler::carrier_admin_router\\(app_state"
---

<objective>
Close the single gap reported in `.planning/phases/02-redis-state-layer/02-VERIFICATION.md`: wire `carrier_admin_router` into the main axum router in `src/main.rs` so the Bearer token auth middleware actually runs on live requests (not just in isolated router tests).

Purpose: Phase 2 implemented `ApiKeyStore`, `auth_middleware`, and `carrier_admin_router` (plan 02-03) and all 9 auth tests pass — but the verifier found that the protected router was never merged into the main axum assembly, leaving auth protection as dead code in production and leaving truth #11 and RAPI-15 only partially satisfied.

Output: `src/main.rs` contains exactly one `.merge(active_call::handler::carrier_admin_router(app_state.clone()))` call inside the main `let app = ...` assembly block, the workspace compiles, and all existing `carrier_admin_router` tests still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-redis-state-layer/02-VERIFICATION.md
@.planning/phases/02-redis-state-layer/02-03-SUMMARY.md
@src/main.rs
@src/handler/handler.rs

<interfaces>
<!-- The exact signature the executor must merge into the main router. -->
<!-- Extracted from src/handler/handler.rs line 78 and src/handler/mod.rs line 17. -->

From src/handler/handler.rs:
```rust
pub fn carrier_admin_router(app_state: AppState) -> Router<AppState> { /* ... */ }
```

From src/handler/mod.rs:
```rust
pub use handler::carrier_admin_router;
```

The function takes an owned `AppState` (not `&AppState`) and returns
`Router<AppState>`, so the call site must pass `app_state.clone()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure carrier_admin_router is merged into the main axum router</name>
  <files>src/main.rs</files>
  <read_first>
    - src/main.rs lines 280-330 (router assembly block — the `let app = active_call::handler::call_router()...with_state(app_state.clone());` chain)
    - src/handler/mod.rs (confirms `pub use handler::carrier_admin_router;`)
    - src/handler/handler.rs lines 74-82 (confirms signature `pub fn carrier_admin_router(app_state: AppState) -> Router<AppState>`)
    - .planning/phases/02-redis-state-layer/02-VERIFICATION.md (the `missing` field — authoritative source for the fix)
  </read_first>
  <action>
Open `src/main.rs` and locate the main router assembly block (currently around lines 314-321):

```rust
let app = active_call::handler::call_router()
    .merge(active_call::handler::playbook_router())
    .merge(active_call::handler::iceservers_router())
    .route("/", get(index))
    .route("/console", get(console))
    .nest_service("/static", ServeDir::new("static"))
    .with_state(app_state.clone());
```

Ensure the following `.merge(...)` line is present immediately after `.merge(active_call::handler::iceservers_router())` and before the `.route("/", get(index))` call, resulting in:

```rust
let app = active_call::handler::call_router()
    .merge(active_call::handler::playbook_router())
    .merge(active_call::handler::iceservers_router())
    .merge(active_call::handler::carrier_admin_router(app_state.clone()))
    .route("/", get(index))
    .route("/console", get(console))
    .nest_service("/static", ServeDir::new("static"))
    .with_state(app_state.clone());
```

Rules:
- The merge line MUST be exactly `.merge(active_call::handler::carrier_admin_router(app_state.clone()))` — do NOT use `&app_state`, do NOT drop the `.clone()` (the function takes an owned `AppState`).
- The line MUST appear inside the main `let app = ...` builder chain, NOT in a separate router that is never used.
- This operation is idempotent: if a spot-check already shows the merge line present (the existing code may already contain it at ~line 317), do NOT add a second copy; simply verify exactly one instance exists and proceed to verification.
- Do NOT modify any other routes, the `.with_state(app_state.clone())` terminator, or surrounding socket/listener logic.
- Do NOT touch `src/handler/handler.rs`, `src/handler/mod.rs`, or any test files — those are already correct per plan 02-03.

Per RAPI-15 and VERIFICATION.md truth #11: this single line wires the auth middleware into the live axum stack so Bearer token protection takes effect on real HTTP requests.
  </action>
  <verify>
    <automated>grep -c "\.merge(active_call::handler::carrier_admin_router(app_state\.clone())" src/main.rs | grep -qx 1 &amp;&amp; cargo check --quiet 2>&amp;1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.merge(active_call::handler::carrier_admin_router(app_state\.clone())" src/main.rs` prints exactly `1` (not 0, not 2)
    - `grep -n "carrier_admin_router(app_state" src/main.rs` shows the match inside the `let app =` block (between the `iceservers_router()` merge and the `.route("/", get(index))` call)
    - `cargo check` exits 0 with no new warnings attributable to this change
    - `src/handler/handler.rs` is unchanged (`git diff --quiet src/handler/handler.rs`)
    - `src/handler/mod.rs` is unchanged (`git diff --quiet src/handler/mod.rs`)
  </acceptance_criteria>
  <done>
`src/main.rs` contains exactly one `.merge(active_call::handler::carrier_admin_router(app_state.clone()))` call inside the main router assembly, the workspace compiles cleanly, and no other files are modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-run auth tests and confirm truth #11 on live router</name>
  <files>(no file modifications — verification only)</files>
  <read_first>
    - src/handler/handler.rs lines 810-850 (existing `carrier_admin_router` tests, including the health-route + auth tests)
    - src/redis_state/auth.rs (auth_middleware + ApiKeyStore tests)
  </read_first>
  <action>
Run the auth-related test suites to confirm that wiring `carrier_admin_router` into `src/main.rs` does not break any existing behavior and that the auth middleware still rejects missing/invalid Bearer tokens and accepts valid ones:

1. Run the handler test module that exercises `carrier_admin_router`:
   ```bash
   cargo test --quiet -p super-voice --lib handler::handler
   ```
2. Run the auth module tests:
   ```bash
   cargo test --quiet -p super-voice --lib redis_state::auth
   ```
3. Run the full workspace test suite to ensure nothing else regressed:
   ```bash
   cargo test --quiet
   ```

If the crate name differs from `super-voice`, derive it from `Cargo.toml` (`[package] name = ...`) and substitute. Do not modify any source files in this task — it is purely a verification gate.

Record the test counts (passed/failed) in the plan SUMMARY that execute-plan will produce.
  </action>
  <verify>
    <automated>cargo test --quiet --lib handler::handler 2>&amp;1 | tail -5 &amp;&amp; cargo test --quiet --lib redis_state::auth 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --lib handler::handler` exits 0 and reports 0 failures
    - `cargo test --lib redis_state::auth` exits 0 and reports 0 failures (9 auth tests from plan 02-03 still pass)
    - `cargo test` (full workspace) exits 0 with 0 failures
    - No test is newly ignored or skipped compared to prior run
  </acceptance_criteria>
  <done>
All existing auth and handler tests pass against the now-wired router. Truth #11 ("API requests with valid Bearer token succeed; requests without/invalid token return 401") is satisfied end-to-end (middleware runs in both unit tests and in the live app stack).
  </done>
</task>

</tasks>

<verification>
Re-run goal-backward verification for Phase 2, specifically targeting:

1. **Truth #11** — "API requests with valid Bearer token succeed; API requests without/invalid Bearer token return 401 Unauthorized"
   - Grep check: `grep -c "\.merge(active_call::handler::carrier_admin_router(app_state\.clone())" src/main.rs` == 1
   - Compile check: `cargo check` exits 0
   - Test check: `cargo test --lib handler::handler` and `cargo test --lib redis_state::auth` both exit 0 with 0 failures
   - Key-link pattern: `grep -E "\.merge\(active_call::handler::carrier_admin_router\(app_state" src/main.rs` matches inside the `let app =` block

2. **Requirement RAPI-15** — "API uses Redis-backed storage with engagement tracking"
   - Was PARTIALLY SATISFIED in 02-VERIFICATION.md because the auth-protected router was not reachable. After this plan, the auth middleware runs on real HTTP requests and RAPI-15 moves to SATISFIED.

3. **Overall phase score** — Should move from 10/11 must-haves verified → 11/11 must-haves verified.

The re-verification pass (run via `/gsd-verify-phase 02` after this plan executes) should regenerate `02-VERIFICATION.md` with `status: verified` and `gaps: []`.
</verification>

<success_criteria>
- [ ] `src/main.rs` contains exactly one `.merge(active_call::handler::carrier_admin_router(app_state.clone()))` call in the main router assembly
- [ ] `cargo check` passes with no new errors or warnings
- [ ] `cargo test --lib handler::handler` passes (0 failures)
- [ ] `cargo test --lib redis_state::auth` passes (0 failures, 9 auth tests still green)
- [ ] `cargo test` (full workspace) passes
- [ ] No files other than `src/main.rs` are modified by this plan
- [ ] Re-running `/gsd-verify-phase 02` reports `gaps: []` and truth #11 as VERIFIED
</success_criteria>

<output>
After completion, create `.planning/phases/02-redis-state-layer/02-04-SUMMARY.md` following the standard summary template. The summary MUST include:

- Confirmation that the `.merge(...carrier_admin_router...)` line is present in `src/main.rs` (with the line number)
- `cargo check` result
- `cargo test` counts (passed / failed) for both `handler::handler` and `redis_state::auth`
- Explicit statement that this closes the single gap from `02-VERIFICATION.md` and lifts truth #11 from PARTIAL to VERIFIED
- Note whether the merge line was already present (idempotent no-op fix) or newly added, so the verification re-run has an accurate audit trail
</output>
