---
phase: 02-redis-state-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/redis_state/mod.rs
  - src/redis_state/pool.rs
  - src/redis_state/config_store.rs
  - src/redis_state/types.rs
  - src/config.rs
  - src/lib.rs
autonomous: true
requirements: [RDIS-01]

must_haves:
  truths:
    - "Redis connection pool initializes from a Redis URL in TOML config"
    - "Dynamic config (endpoint, gateway, trunk, routing, class) can be written to and read from Redis"
    - "Config round-trips through Redis without data loss (serialize then deserialize equals original)"
    - "Application starts with Redis URL in TOML and connects successfully"
  artifacts:
    - path: "src/redis_state/mod.rs"
      provides: "Module root re-exporting pool, config_store, types"
    - path: "src/redis_state/pool.rs"
      provides: "RedisPool wrapper with connection pooling via redis crate ConnectionManager"
      exports: ["RedisPool"]
    - path: "src/redis_state/config_store.rs"
      provides: "ConfigStore with CRUD operations for all entity types using Redis hash/string keys"
      exports: ["ConfigStore"]
    - path: "src/redis_state/types.rs"
      provides: "Serde-serializable types for endpoint, gateway, trunk, routing_table, translation_class, manipulation_class"
      exports: ["EndpointConfig", "GatewayConfig", "TrunkConfig", "RoutingTableConfig", "TranslationClassConfig", "ManipulationClassConfig"]
  key_links:
    - from: "src/redis_state/config_store.rs"
      to: "src/redis_state/pool.rs"
      via: "ConfigStore holds RedisPool"
      pattern: "RedisPool"
    - from: "src/redis_state/config_store.rs"
      to: "src/redis_state/types.rs"
      via: "Serializes entity types to Redis JSON strings"
      pattern: "serde_json::to_string"
    - from: "src/config.rs"
      to: "src/redis_state/pool.rs"
      via: "Config.redis_url feeds RedisPool::new"
      pattern: "redis_url"
---

<objective>
Create the Redis connection infrastructure and config store that persists all dynamic entity configurations to Redis.

Purpose: This is the foundation for the entire Redis state layer. Every subsequent plan in this phase (pub/sub, engagement tracking, API auth) depends on the connection pool and type definitions created here.
Output: `src/redis_state/` module with RedisPool, ConfigStore, and entity type definitions; `redis` crate added to Cargo.toml; `redis_url` field added to Config.
</objective>

<execution_context>
@/Users/parvbhullar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/parvbhullar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/config.rs
@src/app.rs
@Cargo.toml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add redis crate, create entity types, and RedisPool</name>
  <files>Cargo.toml, src/redis_state/mod.rs, src/redis_state/types.rs, src/redis_state/pool.rs, src/config.rs, src/lib.rs</files>
  <behavior>
    - Test: EndpointConfig serializes to JSON and deserializes back to equal struct
    - Test: GatewayConfig, TrunkConfig, RoutingTableConfig, TranslationClassConfig, ManipulationClassConfig all round-trip through serde_json
    - Test: RedisPool::new connects to Redis (use env var REDIS_URL or default redis://127.0.0.1:6379)
    - Test: Config with redis_url field parses from TOML string
  </behavior>
  <action>
    1. Add `redis = { version = "0.28", features = ["tokio-comp", "connection-manager"] }` to [dependencies] in Cargo.toml
    2. Add `pub mod redis_state;` to src/lib.rs
    3. Create src/redis_state/mod.rs re-exporting pool, config_store, types submodules
    4. Create src/redis_state/types.rs with Serde-serializable structs:
       - `EndpointConfig { name: String, stack: String (sofia|rsipstack), transport: String, bind_addr: String, port: u16, tls: Option<TlsConfig>, nat: Option<NatConfig>, auth: Option<AuthConfig>, session_timer: Option<SessionTimerConfig> }`
       - `GatewayConfig { name: String, proxy_addr: String, transport: String, auth: Option<GatewayAuthConfig>, health_check_interval_secs: u32, failure_threshold: u32, recovery_threshold: u32 }`
       - `TrunkConfig { name: String, direction: String, gateways: Vec<GatewayRef>, distribution: String, capacity: Option<CapacityConfig>, codecs: Option<Vec<String>>, acl: Option<Vec<String>> }`
       - `RoutingTableConfig { name: String, rules: Vec<RoutingRule> }`
       - `TranslationClassConfig { name: String, rules: Vec<TranslationRule> }`
       - `ManipulationClassConfig { name: String, rules: Vec<ManipulationRule> }`
       - Each with `#[derive(Debug, Clone, Serialize, Deserialize, PartialEq)]`
       - Sub-structs: TlsConfig, NatConfig, AuthConfig, SessionTimerConfig, GatewayAuthConfig, GatewayRef, CapacityConfig, RoutingRule, TranslationRule, ManipulationRule
    5. Create src/redis_state/pool.rs:
       - `pub struct RedisPool { manager: redis::aio::ConnectionManager }`
       - `RedisPool::new(redis_url: &str) -> Result<Self>` — creates ConnectionManager
       - `RedisPool::get(&self) -> ConnectionManager` — returns clone of manager (ConnectionManager is cheaply cloneable)
    6. Add `redis_url: Option<String>` field to Config in src/config.rs with `#[serde(default)]`
    7. Write unit tests in src/redis_state/types.rs for serde round-trip of each type
  </action>
  <verify>
    <automated>cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice && cargo test redis_state -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>All 6 entity config types round-trip through serde_json. RedisPool compiles. Config parses redis_url from TOML.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ConfigStore with Redis CRUD operations</name>
  <files>src/redis_state/config_store.rs, src/redis_state/mod.rs</files>
  <behavior>
    - Test: ConfigStore::set_endpoint writes JSON to key "endpoint:{name}" and get_endpoint reads it back
    - Test: ConfigStore::list_endpoints returns all endpoints (SCAN pattern "endpoint:*")
    - Test: ConfigStore::delete_endpoint removes the key
    - Test: Same CRUD pattern works for gateway, trunk, routing_table, translation_class, manipulation_class
    - Test: get_endpoint for non-existent key returns None (not error)
  </behavior>
  <action>
    1. Create src/redis_state/config_store.rs:
       - `pub struct ConfigStore { pool: RedisPool }`
       - `ConfigStore::new(pool: RedisPool) -> Self`
       - For each entity type (endpoint, gateway, trunk, routing_table, translation_class, manipulation_class):
         - `set_{entity}(config: &{Entity}Config) -> Result<()>` — SET key "{entity}:{name}" with JSON value
         - `get_{entity}(name: &str) -> Result<Option<{Entity}Config>>` — GET key, deserialize, return None if key missing
         - `list_{entity}s() -> Result<Vec<{Entity}Config>>` — SCAN "{entity}:*", GET each, collect
         - `delete_{entity}(name: &str) -> Result<bool>` — DEL key, return true if deleted
       - Redis key patterns: `endpoint:{name}`, `gateway:{name}`, `trunk:{name}`, `routing_table:{name}`, `translation_class:{name}`, `manipulation_class:{name}`
       - Use `redis::AsyncCommands` trait for async SET/GET/DEL/SCAN
    2. Re-export ConfigStore from src/redis_state/mod.rs
    3. Write integration tests (gated behind `#[cfg(test)]`) that require a running Redis instance. Use `std::env::var("REDIS_URL").unwrap_or("redis://127.0.0.1:6379".into())`. Tests should clean up after themselves (DEL keys in teardown).
    4. Each test uses a unique key prefix (e.g., "test_{uuid}:endpoint:{name}") to avoid collision with parallel test runs. ConfigStore should accept an optional key_prefix in constructor for testing.
  </action>
  <verify>
    <automated>cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice && cargo test config_store -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>ConfigStore CRUD operations work for all 6 entity types. Non-existent keys return None. List returns all matching entities. Delete removes keys.</done>
</task>

</tasks>

<verification>
1. `cargo build` compiles without errors
2. `cargo test redis_state` passes all unit tests (serde round-trips)
3. With a running Redis instance: `cargo test config_store` passes all integration tests
4. Config TOML with `redis_url = "redis://127.0.0.1:6379"` parses correctly
</verification>

<success_criteria>
- redis crate added to workspace dependencies
- 6 entity config types defined with serde Serialize/Deserialize
- RedisPool connects to Redis via ConnectionManager
- ConfigStore provides set/get/list/delete for all entity types using `{entity}:{name}` key pattern
- ConfigStore.list_{entity}s() provides the read-back capability needed for startup restore (actual startup restore logic is Phase 3 scope — when endpoints/gateways are instantiated, the startup path calls list_endpoints/list_gateways to populate runtime state from Redis)
- Config struct accepts optional redis_url from TOML
- All unit tests pass; integration tests pass with running Redis
</success_criteria>

<output>
After completion, create `.planning/phases/02-redis-state-layer/02-01-SUMMARY.md`
</output>
