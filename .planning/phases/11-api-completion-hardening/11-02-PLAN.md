---
phase: 11-api-completion-hardening
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - tests/diagnostics_system_integration.rs
  - tests/regression_integration.rs
autonomous: true
requirements: [RAPI-12, RAPI-13]

must_haves:
  truths:
    - "Diagnostics trunk-test returns gateway reachability results"
    - "Diagnostics route-evaluate returns matched route without placing a call"
    - "System health endpoint reports uptime and Redis connectivity"
    - "System cluster endpoint returns node list"
    - "Existing AI agent call paths are not broken by new carrier routes"
  artifacts:
    - path: "tests/diagnostics_system_integration.rs"
      provides: "Integration tests for diagnostics and system API endpoints"
      min_lines: 100
    - path: "tests/regression_integration.rs"
      provides: "Regression tests verifying AI agent paths work alongside carrier proxy"
      min_lines: 50
  key_links:
    - from: "tests/diagnostics_system_integration.rs"
      to: "src/handler/diagnostics_api.rs"
      via: "HTTP requests to diagnostics endpoints"
      pattern: "/api/v1/diagnostics"
    - from: "tests/diagnostics_system_integration.rs"
      to: "src/handler/system_api.rs"
      via: "HTTP requests to system endpoints"
      pattern: "/api/v1/system"
    - from: "tests/regression_integration.rs"
      to: "src/handler/handler.rs"
      via: "Verifies AI agent routes still exist and respond"
      pattern: "carrier_admin_router"
---

<objective>
Write integration tests that validate all 11 new endpoints work end-to-end and verify no regression in existing AI agent functionality.

Purpose: This is the final phase — integration tests prove the complete API surface works and that adding carrier endpoints did not break the original AI voice agent paths.

Output: Two test files providing end-to-end validation of diagnostics, system, and regression coverage.
</objective>

<execution_context>
@/Users/parvbhullar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/parvbhullar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/handler/handler.rs
@src/handler/diagnostics_api.rs
@src/handler/system_api.rs
@src/app.rs
@tests/carrier_integration.rs
@tests/api_routes_test.rs

<interfaces>
<!-- Executor needs to understand existing test patterns -->

From tests/carrier_integration.rs (typical integration test pattern):
- Build AppState with AppStateBuilder
- Use carrier_admin_router(app_state) to get Router
- Send requests via tower::ServiceExt::oneshot
- Assert status codes and JSON body fields

From src/handler/diagnostics_api.rs (created in Plan 01):
- POST /api/v1/diagnostics/trunk-test: {"trunk": "name"} -> [{gateway, reachable, latency_ms}]
- POST /api/v1/diagnostics/route-evaluate: {"source", "destination", "routing_table"} -> {matched_route, translations, selected_trunk}
- GET /api/v1/diagnostics/registrations -> [{user, registered}]
- GET /api/v1/diagnostics/summary -> {total_gateways, healthy, registrations, active_calls}

From src/handler/system_api.rs (created in Plan 01):
- GET /api/v1/system/health -> {status, uptime_seconds, redis_connected, active_calls}
- GET /api/v1/system/info -> {version, name}
- GET /api/v1/system/cluster -> [{node_id, address}]
- POST /api/v1/system/reload -> {reloaded, timestamp}
- GET /api/v1/system/config -> {non-sensitive config summary}
- GET /api/v1/system/stats -> {total_calls, active_calls, uptime}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integration tests for diagnostics and system endpoints</name>
  <files>tests/diagnostics_system_integration.rs</files>
  <action>
Create `tests/diagnostics_system_integration.rs` with integration tests. Follow the existing test pattern from carrier_integration.rs / api_routes_test.rs:

1. **Helper function**: `build_test_app()` that creates an AppState via AppStateBuilder with minimal config (no Redis required — tests should work without Redis by testing the graceful-degradation paths).

2. **test_system_health_without_redis**: GET /api/v1/system/health returns 200 with `status` field. Without Redis, status should be "degraded" and redis_connected should be false.

3. **test_system_info**: GET /api/v1/system/info returns 200 with `version` and `name` fields. Version matches CARGO_PKG_VERSION.

4. **test_system_stats**: GET /api/v1/system/stats returns 200 with `total_calls`, `active_calls`, `uptime_seconds` fields.

5. **test_system_config**: GET /api/v1/system/config returns 200 with config summary. Should NOT contain any secrets (no redis_url, no api_keys).

6. **test_system_cluster_without_redis**: GET /api/v1/system/cluster returns 200 with at least one node (local).

7. **test_diagnostics_summary**: GET /api/v1/diagnostics/summary returns 200 with summary fields.

8. **test_diagnostics_registrations**: GET /api/v1/diagnostics/registrations returns 200 with array.

9. **test_diagnostics_trunk_test_not_found**: POST /api/v1/diagnostics/trunk-test with `{"trunk": "nonexistent"}` returns 404 or 503 (no config_store).

10. **test_diagnostics_route_evaluate_no_config**: POST /api/v1/diagnostics/route-evaluate with valid body returns 503 when no config_store.

All tests must build a test router WITHOUT the auth middleware layer (or with a valid API key) to avoid 401 responses. Follow whatever auth-bypass pattern exists in the other integration test files. If the existing pattern uses `carrier_admin_router` directly and the tests assert 401 to check route existence, create a separate helper that builds the router without the auth layer for functional testing.

Each test function uses `#[tokio::test]` and follows the tower::ServiceExt::oneshot pattern.
  </action>
  <verify>
    <automated>cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice && cargo test --test diagnostics_system_integration 2>&1 | tail -30</automated>
  </verify>
  <done>All diagnostics and system integration tests pass; endpoints return correct status codes and JSON shapes</done>
</task>

<task type="auto">
  <name>Task 2: Regression tests for AI agent coexistence</name>
  <files>tests/regression_integration.rs</files>
  <action>
Create `tests/regression_integration.rs` to verify that adding the new carrier API endpoints has not broken the original AI agent functionality:

1. **test_ai_agent_routes_still_exist**: Verify that the non-carrier routes (WebSocket call handler, SIP handler, WebRTC handler, playbook endpoints) still exist in the router. Build the full app router (not just carrier_admin_router), send requests to known AI agent paths, verify they return non-404 status codes.

2. **test_carrier_and_agent_routes_coexist**: Build the full router with both AI agent routes and carrier_admin_router merged. Verify that a request to a carrier endpoint (e.g., GET /api/v1/system/health) and a request to an AI agent endpoint (e.g., the health endpoint at /carrier/api/health) both succeed.

3. **test_total_route_count**: Count the total routes in the carrier_admin_router to verify all ~71 routes are present (60 existing + 11 new). This acts as a safety net against accidentally dropped routes.

4. **test_auth_middleware_only_on_carrier_routes**: Verify that the AI agent WebSocket/call endpoints are NOT behind the Bearer auth middleware (they use their own auth), while carrier admin routes ARE behind Bearer auth.

Follow patterns from existing test files. Use `#[tokio::test]` with tower::ServiceExt.
  </action>
  <verify>
    <automated>cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice && cargo test --test regression_integration 2>&1 | tail -20</automated>
  </verify>
  <done>All regression tests pass confirming AI agent paths work alongside carrier admin routes without interference</done>
</task>

</tasks>

<verification>
```bash
cd /Users/parvbhullar/Drives/Vault/Projects/Unpod/super-voice
cargo test --test diagnostics_system_integration --test regression_integration 2>&1 | tail -30
cargo test 2>&1 | tail -10  # Full test suite — no regressions
```
</verification>

<success_criteria>
- All diagnostics endpoint tests pass with correct JSON response shapes
- All system endpoint tests pass with correct JSON response shapes
- AI agent routes confirmed present and functional alongside carrier routes
- Full cargo test suite passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/11-api-completion-hardening/11-02-SUMMARY.md`
</output>
