[package]
name = "pjsip-sys"
version = "0.1.0"
edition = "2024"
description = "Raw FFI bindings to pjproject (pjsip, pjlib, pjsip-ua)"
license = "MIT"
links = "pjsip"

[build-dependencies]
bindgen = "0.71"
pkg-config = "0.3"

[dependencies]
libc = "0.2"
